upstream web_app {
{% for host in groups['webservers'] %}
    server {{ hostvars[host]['ansible_host'] | default(host) }};
{% endfor %}
}

server {
    listen 80;
    location / {
        proxy_pass http://web_app;
    }
}